# Extra autostart processes
# exec-once = uwsm app -- my-service

# Prime fprintd so the fingerprint reader is ready for hyprlock
exec-once = sleep 2 && fprintd-list $USER > /dev/null 2>&1

# Auto-hide waybar (show on mouse at top edge)
exec-once = sleep 1 && ~/.local/bin/omarchy-waybar-autohide
